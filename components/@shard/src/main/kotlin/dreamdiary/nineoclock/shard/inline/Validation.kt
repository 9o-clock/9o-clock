package dreamdiary.nineoclock.shard.inline

//import jakarta.validation.ConstraintViolation
//
//inline fun <T, reified X : Throwable> Set<ConstraintViolation<T>>.isNotEmptyToThrow(supplier: () -> X) {
//    if (isNotEmpty()) throw supplier();
//}
